services:
  audio-service:
    build:
      context: ./services/audio-service
    container_name: gtp-audio-service
    restart: unless-stopped
    environment:
      - PORT=8000
    expose:
      - "8000"

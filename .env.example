# General
CADDY_DOMAIN=planning.made-simple.online
CADDY_EMAIL=mc.dietrich@gmx.de
GHCR_OWNER=marc-dietrich
IMAGE_TAG=latest

# Database
POSTGRES_USER=gtp
POSTGRES_PASSWORD=gtp_pw
POSTGRES_DB=gtp

# OAuth2 Proxy (Google)
OAUTH_CLIENT_ID=your-google-client-id
OAUTH_CLIENT_SECRET=your-google-client-secret
# 32-byte base64 string, e.g. python - <<'PY'
# import secrets, base64
# print(base64.urlsafe_b64encode(secrets.token_bytes(32)).decode())
# PY
OAUTH_COOKIE_SECRET=replace-with-32-byte-base64
# Upstream backend (internal)
OAUTH_UPSTREAM=http://backend:8000
# Public redirect (served via Caddy)
OAUTH_REDIRECT_URL=https://${CADDY_DOMAIN}/oauth2/callback

# Monitoring basic auth for /ops/*
MONITORING_USER=monitor
# Use `caddy hash-password --plaintext "your-password"`
MONITORING_PASSWORD_HASH=replace-with-caddy-bcrypt-hash

# Optional Cloudflare tunnel token (only needed when using profile "tunnel")
CLOUDFLARE_TUNNEL_TOKEN=

# Frontend build-time defaults (override if using a custom domain)
VITE_API_BASE_URL=https://${CADDY_DOMAIN}/api
VITE_OAUTH_BASE_URL=https://${CADDY_DOMAIN}/oauth2

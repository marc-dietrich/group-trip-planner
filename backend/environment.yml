# Group Trip Planner - Conda Environment

name: gtp
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi==0.104.1
      - uvicorn[standard]==0.24.0
      - python-dotenv==1.0.0
      - pydantic==2.5.0
      - sqlmodel==0.0.14
      - asyncpg==0.29.0
      - alembic==1.13.1
